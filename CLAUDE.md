# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 智能体执行规范

以下规则**所有智能体必须严格遵守**，不可违反：

### 语言规范

所有输出、注释、文档、提交信息、代码审查、交互对话均使用**中文**。智能体在创作、生成、修改任何内容时，必须使用中文。

### 注释规范

编写代码时，**每个函数和每个类都必须写注释**，说明其功能与用途。注释格式不做强制要求（JSDoc、Javadoc、Docstring 等均可），但必须做到：

- **函数/方法**：注释说明该函数做什么（而非怎么实现）
- **类/接口**：注释说明该类的职责与用途
- 注释使用中文

### Git 分支规范

- **禁止直接在 `main` 分支上修改代码**
- 每次修改前，先拉取 `main` 分支最新代码
- 从 `main` 创建新的功能/修复分支，在分支上进行开发
- 开发完成后，确认与 `main` 无冲突，再将分支合并回 `main`
- 合并完成后删除开发分支，保持仓库整洁

### 设计参考规范

- **所有开发和设计工作，必须在开工前阅读 [`doc/TTMS软件设计与需求分析.md`](doc/TTMS软件设计与需求分析.md)**
- 该文档整合了需求分析、软件设计、数据模型、接口设计、物理架构等全部关键信息
- 任何代码实现、接口定义、数据库设计、页面开发都必须以该文档为依据
- **开发时必须按照设计文档中的顺序与规定执行**，不得跳过或打乱开发阶段
- 如文档内容与实际需求有出入，需先更新文档再修改代码

### 开发任务与日志规范

- **开发代码必须拆分为小任务**，每个任务粒度控制在可独立完成和验证的范围内
- **每个任务完成后必须写日志记录**，内容包括：做了什么、涉及文件、关键决策
- **日志任务审核通过后，方可提交 Git**，未经审核不得直接 commit

## 项目概述

TTMS（课程设计）—— 一个 B/S 架构的 Web 应用。项目当前处于设计阶段，尚无源代码。

## 设计文档

**首要参考文档**：[`doc/TTMS软件设计与需求分析.md`](doc/TTMS软件设计与需求分析.md) —— 整合了所有 PDF 设计材料的统一文档，所有开发工作以此为准。

设计文档位于 [`doc/`](doc/)：

| 文件 | 内容 |
| ---- | ---- |
| `TTMS软件设计与需求分析.md` | **首要参考**——需求分析 + 软件设计（整合版） |
| `TTMS---需求分析.pdf` | 需求分析（原始 PDF） |
| `UML--活动图.pdf` | UML 活动图（原始 PDF） |
| `TTMS---软件设计(BS).pdf` | 软件设计 B/S 架构（原始 PDF） |
| `需求分析文档.md` | 需求分析（待完善） |
| `前端设计文档.md` | 前端设计（待完善） |
| `服务端设计文档.md` | 服务端设计（待完善） |
| `接口设计文档.md` | 接口设计（待完善） |

PDF 文件为图片类型，自动化工具无法直接提取文字。**以 `TTMS软件设计与需求分析.md` 为主要依据**，其他 `.md` 文件用于补充细节。

## 技术栈

| 类别 | 技术 |
| ------ | ------ |
| 前端 | React 19 + TypeScript + Redux Toolkit + React Router + Axios + Ant Design + Vite + ECharts + Mock.js |
| 后端 | 待定（后续补充） |
| 数据库 | MySQL 5 |
| 架构 | C/S + B/S 混合架构 |
| 建模 | 面向对象 + UML |
| 分层 | User Interface → Filter → Controller → Service → Model → IDAO → DAO → DBUtil |
| 命名约定 | `StudioServlet` / `StudioSrv` / `Studio` / `IStudioDAO` / `StudioDAO` |

## 开发模式

采用**前后端分离 + Mock 驱动开发**模式：

- **接口文档**是前后端的共同契约，双方各自参考同一份接口规范
- **前端**：使用 Mock.js 模拟接口响应，不依赖后端，独立开发与调试
- **后端**：参考接口文档开发真实接口，确保与 Mock 数据格式一致
- 前后端可并行开发，互不阻塞

### 接口开发规范

- **每次开发接口前，必须先查阅接口设计文档**，确认接口的 URL、请求方法、参数、响应格式等要求
- 前端 Mock 模拟和后端真实接口均以接口文档为唯一标准
- 接口实现必须与文档保持一致，不得自行增删字段或修改响应结构
- 如需变更接口，必须先更新接口文档，再同步修改代码

## 注意事项

- PDF 文档为图片类型，自动化工具无法直接提取文字。需要手动查看或使用 OCR。
- 尚未选定构建工具、包管理器或框架——这些决策应在实施规划阶段确定。
