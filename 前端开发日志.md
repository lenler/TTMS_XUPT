# TTMS 前端开发日志

## 2026-05-25：项目脚手架搭建（feat/frontend-init） ✅ 审核通过

### 完成内容

| 项目 | 详情 |
|------|------|
| 分支 | `feat/frontend-init`（从 `main` 创建） |
| 脚手架 | `pnpm create vite ttms-frontend --template react-ts` |
| 依赖安装 | react-router-dom, antd, @ant-design/icons, axios, zustand, msw |
| Vite 配置 | `@` 路径别名 → `src/`，开发服务器端口 3000 |
| TypeScript 配置 | 严格模式，路径别名 `@/*` → `src/*`，关闭 erasableSyntaxOnly（支持 enum） |
| MSW 初始化 | `npx msw init public/`，生成 Service Worker |
| 环境变量 | `.env` 中设置 `VITE_ENABLE_MOCK=true` |

### 目录结构

完整创建了 73 个源文件，覆盖：

- **types/** — `api.ts`（通用响应/分页类型）、`models.ts`（7 个业务实体类型）、`enums.ts`（3 个状态枚举）
- **services/request.ts** — Axios 实例，请求/响应拦截器，token 注入
- **services/admin/** — 10 个 API 服务文件（auth/studio/play/schedule/check/sale/employee/customer/role/finance）
- **services/customer/** — 5 个 API 服务文件（auth/home/schedule/order/board）
- **stores/** — `authStore.ts`（token/用户/菜单持久化）、`cartStore.ts`（选座购物车）
- **routes/** — `admin.routes.tsx`、`customer.routes.tsx`
- **layouts/** — `AdminLayout.tsx`（SaaS 侧边菜单布局）、`CustomerLayout.tsx`（顶栏导航布局）
- **pages/admin/** — 登录页、工作台、11 个模块列表/详情占位页
- **pages/customer/** — 首页、放映安排、选座、订单、支付结果、我的订单、榜单、联系我们、登录、注册
- **components/** — PageTable、SearchForm、StatusTag、SeatEditor 占位
- **hooks/** — useCRUD、usePagination、useAuth 占位
- **mocks/handlers/** — 11 个模块 Handler 占位文件
- **mocks/data/** — 7 个 JSON 数据占位文件

### 验收结果

- `pnpm build`（tsc + vite build）通过，无错误
- `pnpm dev` 可正常启动开发服务器（端口 3000）
- 目录结构与 `doc/前端设计文档.md` 第 3 节一致

### 下一步

任务 2：基础工程能力——类型定义完善 + 路由骨架 + 布局完善 + 路由守卫


## 2026-05-25：路由骨架 + 布局完善 + 路由守卫 + 管理端登录（feat/frontend-routes） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| 管理端路由表 | `routes/admin.routes.tsx` | 17 条路由，包含所有管理模块 |
| 观众端路由表 | `routes/customer.routes.tsx` | 10 条路由，含选座、订单、登录等 |
| AdminLayout | `layouts/AdminLayout.tsx` | 可折叠侧边菜单（6 个一级菜单）、顶栏用户信息、退出按钮、页脚 |
| CustomerLayout | `layouts/CustomerLayout.tsx` | 顶部导航栏（首页/放映/榜单/联系我们）、登录/注册入口、我的订单 |
| AdminGuard | `components/common/AdminGuard.tsx` | 无 token → 跳转 `/admin/login` |
| CustomerGuard | `components/common/CustomerGuard.tsx` | 无 customerToken → 跳转 `/login` |
| 管理端登录页 | `pages/admin/Login.tsx` | 工号+密码表单、表单校验、调用 API、已登录自动跳转 |
| Auth Service | `services/admin/auth.ts` | `adminLogin` / `getCurrentUser` / `getCurrentUserMenus` |
| 登录 Mock | `mocks/handlers/auth.ts` | 成功（admin/123456）、失败（20001）、未登录（20002） |
| Handlers 汇总 | `mocks/handlers/index.ts` | 注册所有模块 handlers |
| App.tsx | `App.tsx` | 管理端+观众端路由组装，Suspense 懒加载全局 Loading |

### 关键设计决策

- **路由方式**：直接 JSX `<Route>` 声明（避免 `RouteObject` spread 的类型兼容问题）
- **登录页独立布局**：`/admin/login` 不走 AdminLayout，只有通过守卫的页面才使用 SaaS 布局
- **观众端 token 独立**：观众端使用 `customerToken`（localStorage 不同 key），与管理端 token 隔离
- **Mock 基准账号**：工号 `admin`，密码 `123456`

### 验收结果

- `pnpm build`（tsc + vite build）通过
- 登录链路：`/admin/login` → 输入 admin/123456 → 跳转 `/admin/dashboard` → 侧边菜单可见
- 路由守卫：直接访问 `/admin/studio` → 跳转 `/admin/login`
- 退出登录：点击退出 → 清除 token → 跳转登录页

### 下一步

任务 3：演出厅管理 CRUD — `PageTable` 通用组件 + `useCRUD` Hook + 列表页 + 新增/修改表单 + Mock 数据


## 2026-05-25：Bug 修复（任务 2 代码审查反馈） ✅ 审核通过

### Bug 修复

| Bug | 文件 | 修复方式 |
|-----|------|---------|
| 退票页菜单高亮失败 | `AdminLayout.tsx:85` | `selectedKey` 从 `slice(0,3)` 截取改为直接取 `location.pathname` 完整路径 |
| 观众端退出不清 token | `CustomerLayout.tsx:52` | "退出"链接添加 `onClick={() => localStorage.removeItem('customerToken')}` |

### 代码质量修复

| 问题 | 文件 | 修复方式 |
|------|------|---------|
| render 期间调用 navigate | `Login.tsx:23` | 改为 `<Navigate to="/admin/dashboard" replace />` |
| 路由文件死代码 | `routes/admin.routes.tsx`、`routes/customer.routes.tsx` | 删除，路由已直接在 `App.tsx` 中以 JSX 声明 |


## 2026-05-25：路由重构 + 演出厅管理 CRUD（任务 3） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| 路由重构 | `App.tsx` | `BrowserRouter` → `createBrowserRouter` + `RouterProvider` |
| 管理端路由表 | `routes/admin.routes.tsx` | `RouteObject[]` 方式声明，含 AdminGuard + AdminLayout 嵌套 |
| 观众端路由表 | `routes/customer.routes.tsx` | `RouteObject[]` 方式声明，含 CustomerGuard 包裹 |
| usePagination | `hooks/usePagination.ts` | page/pageSize/total + setPage/setPageSize/setTotal/resetPage |
| useCRUD | `hooks/useCRUD.ts` | 泛型 Hook，封装 fetchFn 依赖注入，keyword 搜索 + 分页联动 |
| PageTable | `components/common/PageTable.tsx` | 通用分页表格：搜索栏 + 新增按钮 + 编辑/删除操作列 + 删除确认 |
| 演出厅列表 | `pages/admin/Studio/index.tsx` | 使用 PageTable + useCRUD 组装，完整 CRUD 入口 |
| 演出厅详情 | `pages/admin/Studio/Detail.tsx` | Modal 表单（名称/行数/列数/简介），新增修改复用 |
| SeatEditor | `components/admin/SeatEditor.tsx` | 网格编辑器，点击切换 a/\_，配图例和保存按钮 |
| Mock 数据 | `mocks/data/studios.json` | 3 条演出厅初始数据 |
| Mock Handler | `mocks/handlers/studio.ts` | GET 列表/详情、POST 新增、PUT 修改、DELETE 删除、PUT 座位设置 |

### 关键设计决策

- **路由方式**：采用 `createBrowserRouter` + `RouteObject[]` 路由表，支持 `lazy()` 懒加载，管理端和观众端分文件管理
- **通用范式**：`useCRUD(fetchFn)` + `PageTable` 组合，后续所有 CRUD 模块直接复用
- **详情页方式**：Modal 弹窗而非独立路由页面，保持列表上下文
- **SeatEditor**：独立组件，在修改 Modal 内嵌显示

### 验收结果

- `pnpm build`（tsc + vite build）通过
- 演出厅列表分页、搜索、新增、修改、删除完整链路
- 修改演出厅时可编辑座位布局
- Mock 数据正确响应 CRUD 操作

### 下一步

任务 4：剧目管理 + 演出计划 CRUD（复用 PageTable + useCRUD 范式）


## 2026-05-25：Bug 修复（任务 3 代码审查反馈） ✅ 审核通过

| 编号 | 类型 | 文件 | 修复 |
|------|------|------|------|
| Bug 1 | 分页不请求 | `Studio/index.tsx` | `handlePageChange` 追加 `refresh({ page, keyword })` |
| Bug 2 | 首页菜单不高亮 | `AdminLayout.tsx` | `/admin` 路径映射为 `/admin/dashboard` |
| 质量 | FetchFn 返回 any | `services/admin/studio.ts` | service 函数添加显式 `Promise<ApiResponse<...>>` 返回类型 |


## 2026-05-25：剧目管理 CRUD + useCRUD 增强（任务 4） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| Mock 数据 | `mocks/data/plays.json` | 4 条剧目（雷雨/歌剧魅影/红色娘子军/茶馆） |
| Mock Handler | `mocks/handlers/play.ts` | CRUD + 数据字典（剧目类型/语种）|
| API Service | `services/admin/play.ts` | getPlays / getPlayById / createPlay / updatePlay / deletePlay / getDicts |
| 列表页 | `pages/admin/Play/index.tsx` | 表格 + 关键词搜索 + 类型/语种下拉筛选 |
| 详情 Modal | `pages/admin/Play/Detail.tsx` | 名称/类型/语种/时长/票价/海报/宣传片/简介 8 字段 |
| useCRUD 增强 | `hooks/useCRUD.ts` | 新增 `extraParams` + `setExtraParams`，筛选参数并入请求 |
| 路由清理 | `routes/admin.routes.tsx` | 移除 PlayDetailPage（已改为 Modal） |

### 验收结果

- `pnpm build` 通过
- 列表展示 + 关键词搜索 + 类型/语种筛选联动，分页正确
- 新增/修改 Modal 表单，字典下拉数据正确
- 删除二次确认 + 刷新列表

### 下一步

任务 5：演出计划 CRUD（选择剧目 + 演出厅 + 时间冲突检测 Mock）


## 2026-05-25：演出计划管理 CRUD（任务 5） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| Mock 数据 | `mocks/data/schedules.json` | 2 条初始数据（雷雨/1号厅、歌剧魅影/2号厅） |
| Mock Handler | `mocks/handlers/schedule.ts` | CRUD + 前后 2 小时冲突检测（同一演出厅） |
| API Service | `services/admin/schedule.ts` | getSchedules / getScheduleById / createSchedule / updateSchedule / deleteSchedule |
| 列表页 | `pages/admin/Schedule/index.tsx` | 表格 + 剧目/演出厅下拉筛选 |
| 详情 Modal | `pages/admin/Schedule/Detail.tsx` | 剧目搜索下拉 + 演出厅选择 + 日期时间 + 票价 + 冲突错误提示 |
| 依赖 | `package.json` | 新增 dayjs（DatePicker 值转换） |
| 路由清理 | `routes/admin.routes.tsx` | 移除 ScheduleDetailPage（已改为 Modal） |

### 关键设计决策

- **冲突检测**：Mock 端实现同一演出厅 ±2 小时内已有排期即返回 `20005`
- **DatePicker 值处理**：用 dayjs 做 `YYYY-MM-DD HH:mm:ss` 格式双向转换
- **错误展示**：Modal 内嵌 `Alert` 组件，冲突时显示红色提示可关闭

### 验收结果

- `pnpm build` 通过
- 列表展示 + 剧目/演出厅筛选
- 新增时选择冲突时段返回 20005 错误提示
- 修改时排除自身做冲突检测
- 删除二次确认生效

### 下一步

任务 6：员工/观众管理 CRUD（复用 PageTable + Modal 范式）

## 2026-05-26：员工管理 + 观众管理 CRUD（任务 6） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| Mock 数据 | `mocks/data/employees.json` | 3 条员工数据（售票员/运营经理/系统管理员） |
| Mock 数据 | `mocks/data/customers.json` | 3 条观众数据（正常/正常/已封禁） |
| Mock Handler | `mocks/handlers/employee.ts` | 列表（去密码）/详情/新增/修改（密码可选）/删除，岗位 7 种 |
| Mock Handler | `mocks/handlers/customer.ts` | 列表/详情/封禁解封状态切换 |
| API Service | `services/admin/employee.ts` | getEmployees/getEmployeeById/createEmployee/updateEmployee/deleteEmployee |
| API Service | `services/admin/customer.ts` | getCustomers/getCustomerById/updateCustomerStatus |
| 列表页 | `pages/admin/Employee/index.tsx` | 表格 + 关键词搜索 + 岗位筛选 + 新增/编辑/删除 |
| Modal 表单 | `pages/admin/Employee/Detail.tsx` | 工号/姓名/性别/岗位/电话/邮箱/密码 7 字段，新增密码必填修改选填 |
| 列表页 | `pages/admin/Customer/index.tsx` | 表格 + 搜索 + Drawer 详情 + 封禁/解封操作 |
| 通用组件 | `components/common/StatusTag.tsx` | 状态码 → 彩色 Tag，支持自定义映射 |
| PageTable 增强 | `components/common/PageTable.tsx` | 新增 `renderActions` prop 支持自定义操作列 |
| 类型补充 | `types/models.ts` | 新增 `Customer` 接口 |
| 路由清理 | `routes/admin.routes.tsx` | 移除 `employee/:id` 路由（已改为 Modal） |

### 关键设计决策

- **员工 CRUD 范式**：完全复用 Studio/Play/Schedule 的 `useCRUD + PageTable + Modal` 模式
- **观众管理**：不走 CRUD Modal（无新增/编辑），改为 Drawer 查看详情 + 封禁/解封按钮
- **岗位字典**：Mock 阶段在 handler 中硬编码 7 种岗位映射，列表和表单共用
- **密码处理**：列表接口不返回密码，修改时空则不变更原密码
- **PageTable 扩展**：新增 `renderActions` 可选 prop，支持自定义操作列替代默认编辑/删除

### 验收结果

- `pnpm build`（tsc + vite build）通过，无类型错误
- 员工 CRUD 完整链路：列表 → 搜索 → 岗位筛选 → 新增 → 编辑 → 删除
- 观众管理链路：列表 → 搜索 → 查看详情 → 封禁/解封

### 下一步

任务 7：售票/退票 + 验票管理


## 2026-05-26：售票管理 + 退票处理 + 验票管理（任务 7） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| Schedule 改造 | `mocks/handlers/schedule.ts` | 新增计划时自动生成票（按 studio 座位布局）；新增 `GET /admin/api/schedules/:id/tickets` 接口；删除计划时级联删票；导出共享 `ticketsStore` |
| Sale Service | `services/admin/sale.ts` | createSale / getSales / getSaleById / refundSale |
| Check Service | `services/admin/check.ts` | getChecks / verifyTicket |
| Sale Mock | `mocks/handlers/sale.ts` | 售票校验（票状态/场次/金额）→ 更新票为 sold → 创建 Sale + SaleItem；退票校验（订单/票状态）→ 票 refunded → 订单状态更新 |
| Check Mock | `mocks/handlers/check.ts` | 验票校验（票存在/状态/时间窗口）→ 票 checked；记录验票日志 |
| Schedule Service 补充 | `services/admin/schedule.ts` | 新增 `getScheduleTickets` 函数（查询场次票/座位） |
| 售票页 | `pages/admin/Sale/index.tsx` | 销售记录表格 + 新增售票 Modal（900px 左右分栏）：左侧选场次→加载座位图→点击选座，右侧汇总+选顾客+实收金额+找零 |
| 退票页 | `pages/admin/Sale/Refund.tsx` | 销售记录表格 + 退票 Modal：Checkbox 多选票 + 退款金额展示 + 确认退票 |
| 验票页 | `pages/admin/Check/index.tsx` | 上部验票操作区（输入票ID→验票→Result展示）+ 下部验票记录表格 |

### 关键设计决策

- **票运行期生成**：新增演出计划时，按 `studio.rowCount × colCount` 自动生成 `TicketStore` 对象，每个座位一行，初始状态 available(0)
- **跨 Handler 共享**：ticketsStore 从 schedule handler 导出，sale/check handler 直接 import 使用同一引用
- **售票校验顺序**：票存在 → 票状态 available → 票归属正确场次 → 收款充足 → 出票
- **退票部分支持**：支持部分退票——退部分票后订单仍为 paid，全部退完后变为 refunded
- **验票结果双展示**：Card 内嵌 `Result` 组件（绿色/红色），同时记录持久化到验票日志

### 验收结果

- `pnpm build`（tsc + vite build）通过，无类型错误
- 票务链路完整：新增演出计划 → 自动生成 49 张票 → 售票选座 → 票变为 sold → 验票通过 → 票变为 checked
- 退票链路：销售记录 → 退票 → 票变为 refunded
- 边界校验：已售票不可再售、已验票不可退票、收款不足拒绝售票

### 下一步

任务 8：角色权限管理 + 财务统计（或按需调整优先级）


## 2026-05-26：角色权限管理（任务 8） ✅ 审核通过

### 完成内容

| 模块 | 文件 | 说明 |
|------|------|------|
| Role Service | `services/admin/role.ts` | getRoles / createRole / updateRole / deleteRole / getResources / assignEmployeeRoles |
| Role Mock | `mocks/handlers/role.ts` | 2 条初始角色（系统管理员全权限 / 售票员限权限）+ 11 条静态资源 + `buildMenuTree` 导出（供 auth handler 复用） |
| 角色列表 | `pages/admin/Role/index.tsx` | 表格 + 角色名称/资源数/资源Tag + 新增/编辑/删除 |
| 角色 Modal | `pages/admin/Role/Detail.tsx` | 名称 Input + 资源 Checkbox.Group 按父级分组 Card + 全选/取消 |
| Auth Mock 增强 | `mocks/handlers/auth.ts` | 登录支持多用户（admin/EMP001）；菜单接口按 roleIds 调用 `buildMenuTree` 返回角色对应的菜单树 |
| authStore 改造 | `stores/authStore.ts` | `fetchMenus` 调用 `getCurrentUserMenus()` 并存储返回的 menus |
| AdminLayout 改造 | `layouts/AdminLayout.tsx` | **菜单从硬编码 → authStore 动态读取**；icon 映射表（菜单名→antd 图标）；首次加载调用 fetchMenus；加载中显示 Spin |
| 员工角色分配 | `pages/admin/Employee/Detail.tsx` | 修改模式下新增"角色分配"Checkbox.Group；保存时调用 `assignEmployeeRoles` |
| 路由清理 | `routes/admin.routes.tsx` | 移除废弃的 `role/:id` 路由（已改为 Modal） |

### 关键设计决策

- **菜单动态化**：AdminLayout 不再维护静态 75 行 `menuItems` 数组，改为从 `authStore.menus` 读取。登录后调用 `fetchMenus()` → `GET /admin/api/current-user/menus` → Mock 根据用户 roleIds 返回不同菜单树
- **buildMenuTree 共享**：role handler 导出 `buildMenuTree(resourceIds)` 函数，auth handler 调用它生成菜单，避免资源→菜单映射逻辑重复
- **多用户登录**：auth mock 支持 `admin`（系统管理员）和 `EMP001`（售票员）两个账号，售票员登录后仅显示工作台/售票/退票/观众管理 4 项菜单
- **资源分组展示**：角色 Modal 按 `parentName` 分组渲染 Checkbox，每组一张 Card

### 验收结果

- `pnpm build`（tsc + vite build）通过，无类型错误
- 角色 CRUD 完整链路：列表 → 新增（含资源勾选） → 修改 → 删除
- 菜单动态化：admin 登录 → 11 项全菜单；EMP001 登录 → 4 项菜单（售票员权限）
- 员工角色分配：编辑员工 → 勾选角色 → 保存

### 下一步

任务 9：Dashboard 工作台 + 财务统计